# Database
# For Vercel deployment, use a connection pooling service like PgBouncer or Supabase's connection pooling
# Format: postgresql://user:password@pooler-host:port/database?schema=public&pgbouncer=true
DATABASE_URL=postgresql://user:password@localhost:5432/swasthiya_db?schema=public

# JWT Configuration
# In production, use a strong random key
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-use-a-strong-random-string

# Environment
NODE_ENV=development

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000
